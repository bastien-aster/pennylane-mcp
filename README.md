# Pennylane MCP Server

Serveur MCP (Model Context Protocol) pour l'API Pennylane.

## 🚀 Installation

```bash
# Cloner le repo
git clone <repo-url>
cd pennylane-mcp

# Installer avec uv (recommandé)
uv pip install -e .

# Ou avec pip
pip install -e .